services:
  frontend:
    container_name: frontend
    image: 'frontend:latest'
    build: .
    ports:
      - "4200:80"
    restart: always
    networks:
        - frontend-network
networks:
    frontend-network:
        driver: bridge



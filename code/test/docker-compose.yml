version: "3.8"
services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8084:8084"
  angular-app:
    build: .
    platform: linux/arm/v7
    ports:
      - "5000:80"
    restart: unless-stopped
networks:
    trd-net:
      driver: bridge


    
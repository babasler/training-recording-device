version: '3.8'

services:
  backend:
    build: ./backend
    networks:
      - app-network

  frontend:
    build: ./frontend
    ports:
      - "8084:8084"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
